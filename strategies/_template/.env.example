# Copy this file to .env and fill in the values. Never commit .env.
# The code does not read this file. Export the variables before each run:
#   set -a; source .env; set +a
# List every variable the strategy reads, with a comment on where to get it.
EXAMPLE_API_KEY=
