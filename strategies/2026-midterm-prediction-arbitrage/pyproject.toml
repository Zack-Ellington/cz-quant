[project]
name = "2026-midterm-prediction-arbitrage"
version = "0.1.0"
description = "Arbitrage on Kalshi prediction markets for the 2026 US midterm elections."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0",
]

[project.scripts]
strategy = "strategy.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/strategy"]
